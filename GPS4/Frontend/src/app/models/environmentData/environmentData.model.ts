import { WeatherData } from "../weatherData/weatherData.model";
import { Environment } from "../environment/environment.model";

export class EnvironmentData {
    weather: WeatherData;
    environment : Environment;
}